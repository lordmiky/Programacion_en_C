#include "graphics.h"
int main(){
	int gd=DETECT, gm;
	initgraph(&gd, &gm, "\\bgi");
	setcolor(GREEN);
	rectangle(40,40, 100, 100);
	setcolor(BLUE);
	rectangle(100,100, 190, 190);
	setcolor(RED);
	circle(190, 190, 20);
	getch();
	closegraph();
	return 0;
}
